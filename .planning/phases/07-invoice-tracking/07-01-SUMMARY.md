---
phase: 07-invoice-tracking
plan: 01
subsystem: data
tags: [ndjson, json-schema, xero, invoicing, commands]

# Dependency graph
requires:
  - phase: 06-daily-todos
    provides: "Schema/command/build-script pattern (todo-record.json, /todo, build-dashboard-data.sh)"
provides:
  - "Invoice NDJSON schema (schemas/invoice-record.json) with 18 fields"
  - "Invoice data file (data/invoices/active.jsonl)"
  - "/invoice command with 7 modes including Xero sync"
  - "Build script extension producing docs/invoices.json"
affects: [07-02, 07-03, dashboard, briefing, triage]

# Tech tracking
tech-stack:
  added: ["@xeroapi/xero-mcp-server (configured in command, not yet installed)"]
  patterns: ["Dual data flow: email reminders + Xero sync", "Overdue computed dynamically (never stored)", "Xero status mapping to local lifecycle"]

key-files:
  created:
    - schemas/invoice-record.json
    - data/invoices/active.jsonl
    - .claude/commands/invoice.md
  modified:
    - scripts/build-dashboard-data.sh

key-decisions:
  - "Overdue is computed (status==sent AND due_date<today), never stored as a status enum value per D-02"
  - "Invoice schema has 18 fields covering D-04 through D-08 with additionalProperties:false"
  - "/invoice command has 7 modes: show, list, mark-paid, sync, overdue, link, create"

patterns-established:
  - "Invoice ID pattern: inv-YYYY-MM-DD-NNN"
  - "Xero status mapping: DRAFT/SUBMITTED->draft, AUTHORISED->sent, PAID->paid, VOIDED->written-off"
  - "Graceful MCP unconfigured handling: show setup instructions instead of crashing"

requirements-completed: [INV-01, INV-02]

# Metrics
duration: 3min
completed: 2026-03-23
---

# Phase 7 Plan 1: Invoice Data Layer Summary

**Invoice NDJSON schema with 18 fields, /invoice command with 7 modes (show/list/mark-paid/sync/overdue/link/create), Xero MCP sync, and build script producing invoices.json**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-23T12:17:05Z
- **Completed:** 2026-03-23T12:20:46Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Invoice schema with all 18 fields from decisions D-04 through D-08, overdue computed not stored
- /invoice command with 7 modes including Xero MCP sync with graceful unconfigured handling
- Build script extended to compile invoices.json for dashboard consumption
- Data directory and empty NDJSON file ready for invoice records

## Task Commits

Each task was committed atomically:

1. **Task 1: Invoice schema, data directory, and build script extension** - `fecc9e1` (feat)
2. **Task 2: /invoice command with all modes** - `311cf30` (feat)

## Files Created/Modified
- `schemas/invoice-record.json` - Invoice NDJSON schema with 18 fields, additionalProperties:false
- `data/invoices/active.jsonl` - Empty invoice data file (NDJSON)
- `.claude/commands/invoice.md` - /invoice command with 7 modes (523 lines)
- `scripts/build-dashboard-data.sh` - Added invoices.json compilation block
- `docs/invoices.json` - Empty array (generated by build script)

## Decisions Made
- Overdue is computed dynamically (status=="sent" AND due_date < today), never stored as enum value -- follows D-02 and anti-pattern warning from research
- Schema uses exactly the 6 statuses from D-01: reminder, draft, sent, paid, disputed, written-off
- Xero MCP sync mode includes graceful handling when server is not configured (shows setup steps)
- Project code displayed before client name in all output formats per D-08 user specifics

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

Xero MCP server requires manual configuration before /invoice sync will work. The command includes inline setup instructions when Xero is not configured. Full setup details are in the plan's user_setup section:
- Create Custom Connection app in Xero Developer Portal
- Select scopes: accounting.invoices.read, accounting.contacts.read
- Add MCP server config to ~/.claude.json with XERO_CLIENT_ID and XERO_CLIENT_SECRET

## Next Phase Readiness
- Schema and command ready for triage pipeline integration (07-02)
- Build script ready for dashboard tab addition (07-03)
- Xero sync mode ready once MCP server is configured by user

## Self-Check: PASSED

All 6 files verified present. Both task commits verified in git log.

---
*Phase: 07-invoice-tracking*
*Completed: 2026-03-23*
