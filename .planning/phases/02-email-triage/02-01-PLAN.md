---
phase: 02-email-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/triage-record.json
  - data/config/clients.jsonl
  - .claude/agents/email-scanner.md
autonomous: true
requirements: [EMAIL-04, EMAIL-08]
must_haves:
  truths:
    - "Triage records can store client name, client domain, structured action type, and draft ID per email"
    - "A seed list of client domains exists that the triage engine can load and match against"
    - "An email-scanner subagent definition exists with correct tool permissions for Gmail MCP access"
  artifacts:
    - path: "schemas/triage-record.json"
      provides: "Extended triage record schema with client and action fields"
      contains: "client_name"
    - path: "data/config/clients.jsonl"
      provides: "Client domain seed list for rules-based priority boosting"
      min_lines: 3
    - path: ".claude/agents/email-scanner.md"
      provides: "Subagent definition with YAML frontmatter and tool permissions"
      contains: "mcp__hardened-workspace__"
  key_links:
    - from: "data/config/clients.jsonl"
      to: ".claude/agents/email-scanner.md"
      via: "Subagent reads client list from data/config/clients.jsonl"
      pattern: "data/config/clients.jsonl"
    - from: "schemas/triage-record.json"
      to: "data/triage/"
      via: "Triage JSONL files must conform to this schema"
      pattern: "triage-record"
---

<objective>
Extend the Phase 1 triage record schema with fields needed for client tagging and structured action detection, create the client domain seed list, and define the email-scanner subagent with correct MCP tool permissions.

Purpose: Establish data contracts and configuration before implementing the triage engine. Plan 02 builds the subagent prompt against these contracts.
Output: Extended schema, populated client config, subagent definition file with tool restrictions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-email-triage/02-CONTEXT.md
@.planning/phases/02-email-triage/02-RESEARCH.md
@schemas/triage-record.json
@schemas/feed-entry.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend triage-record.json schema with client and action fields</name>
  <files>schemas/triage-record.json</files>
  <read_first>schemas/triage-record.json, schemas/feed-entry.json, .planning/phases/02-email-triage/02-RESEARCH.md</read_first>
  <action>
Extend the existing triage-record.json schema (JSON Schema draft-07) with four new optional properties. The schema currently has `"additionalProperties": false` so these MUST be added explicitly to the `properties` object.

Add these fields to the `properties` object (per D-13 client tagging and D-14 structured actions):

1. `"client_name"` - type: string, description: "Matched client name from data/config/clients.jsonl (null if no match)"
2. `"client_domain"` - type: string, description: "Matched client domain from data/config/clients.jsonl (null if no match)"
3. `"action_type"` - type: string, enum: ["contract", "invoice", "meeting", "deadline", "none"], description: "Primary detected action type per D-13 (contract/legal, invoice, meeting request, deadline mention)"
4. `"draft_id"` - type: string, description: "Gmail draft ID if a draft reply was created for this email"

Do NOT add these to the `required` array -- they are optional fields. Do NOT change any existing fields or their definitions. Keep the `"additionalProperties": false` setting.

Validate the resulting JSON is well-formed: `jq '.' schemas/triage-record.json`
  </action>
  <verify>
    <automated>jq '.properties | keys' /Users/glenr/work/todo-list/schemas/triage-record.json | grep -q 'client_name' && jq '.properties | keys' /Users/glenr/work/todo-list/schemas/triage-record.json | grep -q 'action_type' && jq '.properties | keys' /Users/glenr/work/todo-list/schemas/triage-record.json | grep -q 'draft_id' && jq '.properties | keys' /Users/glenr/work/todo-list/schemas/triage-record.json | grep -q 'client_domain' && echo "PASS: All 4 new fields present" || echo "FAIL: Missing fields"</automated>
  </verify>
  <acceptance_criteria>
- `jq '.properties.client_name.type' schemas/triage-record.json` outputs `"string"`
- `jq '.properties.client_domain.type' schemas/triage-record.json` outputs `"string"`
- `jq '.properties.action_type.enum' schemas/triage-record.json` outputs `["contract","invoice","meeting","deadline","none"]`
- `jq '.properties.draft_id.type' schemas/triage-record.json` outputs `"string"`
- `jq '.required' schemas/triage-record.json` still contains exactly: `["message_id","thread_id","from","subject","received","priority"]` (unchanged)
- `jq '.additionalProperties' schemas/triage-record.json` outputs `false`
  </acceptance_criteria>
  <done>Triage record schema has client_name, client_domain, action_type (5-value enum), and draft_id as optional properties. Existing fields unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create client domain seed list and email-scanner subagent definition</name>
  <files>data/config/clients.jsonl, .claude/agents/email-scanner.md</files>
  <read_first>schemas/triage-record.json, .planning/phases/02-email-triage/02-RESEARCH.md, .planning/phases/02-email-triage/02-CONTEXT.md, .claude/commands/triage-inbox.md</read_first>
  <action>
**Part A: Create data/config/clients.jsonl**

Create the client domain seed list at `data/config/clients.jsonl` (per D-03). This file uses NDJSON format (one JSON object per line). Include 3 example/placeholder entries that Glen can customize. Each line must have this structure:

    {"domain": "example-client.com", "name": "Example Client Pty Ltd", "aliases": ["example-client.com.au"], "contact": "Jane Smith"}

Fields:
- `domain` (string, required): Primary email domain for this client
- `name` (string, required): Human-readable client name for display in triage results
- `aliases` (array of strings, required): Additional domains that belong to this client (subdomains, country variants)
- `contact` (string, required): Primary contact person name at this client

Create exactly 3 placeholder entries with clearly fake domains (e.g., `acme-corp.com`, `widgets-inc.com.au`, `example-foundation.org`) so Glen knows the format and can replace them with real client data.

**Part B: Create .claude/agents/email-scanner.md**

First create the `.claude/agents/` directory if it does not exist.

Create the subagent definition file at `.claude/agents/email-scanner.md` with YAML frontmatter and a SKELETON system prompt. The full system prompt will be written in Plan 02 -- this task establishes the file with correct tool permissions and a placeholder body.

The file must start with YAML frontmatter (between triple-dash delimiters) containing exactly these fields:
- name: email-scanner
- description: Scans Gmail inbox via hardened MCP, triages emails by priority, generates draft replies for urgent/client emails
- tools: Read, Write, Edit, Bash(jq *), Bash(date *), mcp__hardened-workspace__search_gmail_messages, mcp__hardened-workspace__get_gmail_message_content, mcp__hardened-workspace__get_gmail_messages_content_batch, mcp__hardened-workspace__draft_gmail_message, mcp__hardened-workspace__list_gmail_labels
- model: sonnet

After the frontmatter, add a skeleton body:

    You are an email triage agent for Glen, founder of Agend Systems (~30 clients).

    PLACEHOLDER: Full system prompt will be implemented in Phase 2 Plan 02.

    This subagent will:
    1. Scan Gmail inbox (last 24 hours) using hardened MCP tools
    2. Load client domains from data/config/clients.jsonl
    3. Classify emails using rules + AI hybrid approach
    4. Surface starred emails as highest priority
    5. Detect actionable items (contracts, invoices, meetings, deadlines)
    6. Generate Gmail draft replies for urgent/client emails
    7. Write triage results to data/triage/{timestamp}.jsonl
    8. Log summary to data/feed.jsonl

Do NOT include tools that are not in the list above. Specifically, do NOT include `mcp__hardened-workspace__send_gmail_message` (removed from hardened fork) or any Drive/Calendar tools.
  </action>
  <verify>
    <automated>test -f /Users/glenr/work/todo-list/data/config/clients.jsonl && wc -l < /Users/glenr/work/todo-list/data/config/clients.jsonl | tr -d ' ' | grep -q '^3$' && jq -e '.' /Users/glenr/work/todo-list/data/config/clients.jsonl > /dev/null 2>&1 && test -f /Users/glenr/work/todo-list/.claude/agents/email-scanner.md && grep -q 'mcp__hardened-workspace__search_gmail_messages' /Users/glenr/work/todo-list/.claude/agents/email-scanner.md && grep -q 'mcp__hardened-workspace__draft_gmail_message' /Users/glenr/work/todo-list/.claude/agents/email-scanner.md && grep -q 'model: sonnet' /Users/glenr/work/todo-list/.claude/agents/email-scanner.md && ! grep -q 'send_gmail_message' /Users/glenr/work/todo-list/.claude/agents/email-scanner.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `data/config/clients.jsonl` exists with exactly 3 lines
- Each line in `data/config/clients.jsonl` is valid JSON (passes `jq -e '.'`)
- Each line contains keys: `domain`, `name`, `aliases`, `contact`
- `.claude/agents/email-scanner.md` exists
- File starts with YAML frontmatter (triple-dash delimiter)
- Contains `name: email-scanner`
- Contains `model: sonnet`
- Contains `mcp__hardened-workspace__search_gmail_messages` in tools
- Contains `mcp__hardened-workspace__get_gmail_messages_content_batch` in tools
- Contains `mcp__hardened-workspace__draft_gmail_message` in tools
- Does NOT contain `send_gmail_message` anywhere in the file
- Does NOT contain `mcp__hardened-workspace__search_drive_files` or any Drive tools
  </acceptance_criteria>
  <done>Client domain seed list created with 3 placeholder entries. Email-scanner subagent file created with correct YAML frontmatter, tool permissions, and model setting. No send/share/filter/Drive tools included.</done>
</task>

</tasks>

<verification>
1. `jq '.' schemas/triage-record.json` -- valid JSON with 4 new optional fields
2. `jq -e '.' data/config/clients.jsonl` -- each line is valid JSON
3. `head -5 .claude/agents/email-scanner.md` -- shows YAML frontmatter with correct tools
4. `grep -c 'send_gmail_message' .claude/agents/email-scanner.md` -- returns 0 (security check)
5. `scripts/validate-data.sh` -- passes (no data corruption)
</verification>

<success_criteria>
- Triage record schema extended with client_name, client_domain, action_type (5-value enum), draft_id
- Client seed list exists at data/config/clients.jsonl with 3 entries in correct NDJSON format
- Email-scanner subagent defined at .claude/agents/email-scanner.md with restricted MCP tool set
- No security-removed tools (send, share, filter, delete) appear in any file
- All existing schemas and data files remain valid
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-triage/02-01-SUMMARY.md`
</output>
