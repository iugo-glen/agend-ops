---
phase: 03-task-execution
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - data/tasks/active.jsonl
  - data/feed.jsonl
autonomous: false
gap_closure: true
requirements: [TASK-04, TASK-05]
must_haves:
  truths:
    - "Task execution produces output files in data/tasks/{task-id}/ directory"
    - "Task record output_dir is populated after completion"
    - "Gmail draft is created for email-triggered tasks with draft_id recorded"
    - "Activity feed logs task completion with output_dir in details"
  artifacts:
    - path: "data/tasks/*/summary.md"
      provides: "Markdown analysis output from document-summary task type"
      min_lines: 10
    - path: "data/tasks/active.jsonl"
      provides: "Task records with output_dir populated for completed tasks"
      contains: "output_dir"
  key_links:
    - from: ".claude/agents/task-executor.md"
      to: "data/tasks/{task-id}/"
      via: "STEP 4 creates output directory and writes markdown files"
      pattern: "mkdir -p data/tasks/"
    - from: ".claude/agents/task-executor.md"
      to: "data/tasks/active.jsonl"
      via: "STEP 5a updates task record with output_dir, status=completed"
      pattern: "output_dir"
---

<objective>
Run a task through the full execution pipeline (including STEP 4 Save Output and STEP 5 Complete Task) to produce actual output files and prove the task-executor delivers end-to-end.

Purpose: Closes Gap 1 from VERIFICATION.md -- task-2026-03-23-001 completed with output_dir=null and no output files because STEP 4 did not execute during the inline fallback path. This plan also fixes the existing incomplete task record. The subagent code is correct; it just needs to be exercised fully.

Output: At least one data/tasks/{task-id}/ directory with markdown output file(s), task record with output_dir populated, optional Gmail draft with draft_id
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-task-execution/03-CONTEXT.md
@.planning/phases/03-task-execution/03-01-SUMMARY.md
@.planning/phases/03-task-execution/03-02-SUMMARY.md
@.planning/phases/03-task-execution/03-VERIFICATION.md

<interfaces>
<!-- Task executor STEP 4 and STEP 5 from .claude/agents/task-executor.md -->

STEP 4 (Save Output):
  1. mkdir -p data/tasks/{task-id}/
  2. Write primary output file based on task_type:
     - contract-review -> analysis.md
     - meeting-prep -> talking-points.md (primary) + context.md (secondary)
     - document-summary -> summary.md
     - draft-comms -> draft.md
  3. Markdown formatted: headers, bullets, bold for key terms

STEP 5 (Complete Task):
  5a. Update task record: status=completed, outcome=summary, completed_at=timestamp, output_dir=path, task_type=resolved
  5b. If source_email set: create Gmail draft via draft_gmail_message with in_reply_to threading, record draft_id
  5c. Append feed entry: type=task, summary, details including output_dir

Existing incomplete task:
  task-2026-03-23-001: status=completed, output_dir=null, no output directory
  This task has source_email=19d17c8f98f99484 (Greg Davenport PCA SOW email)

Available from Plan 03: triage-suggestion tasks in active.jsonl (pending, ready to execute)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix incomplete task and run a triage-suggested task to full completion</name>
  <files>data/tasks/active.jsonl, data/feed.jsonl</files>
  <read_first>.claude/agents/task-executor.md, .claude/commands/task.md, data/tasks/active.jsonl, schemas/task-record.json</read_first>
  <action>
This task has two parts: fix the existing broken task record, then execute a triage-suggested task through the full pipeline.

**Part A: Fix task-2026-03-23-001 (the incomplete completed task)**

The existing task-2026-03-23-001 has status=completed but output_dir=null and no output files. This is inconsistent. Reset it so the record is honest:

1. Read data/tasks/active.jsonl
2. Find the record for task-2026-03-23-001
3. Update its status to "pending" (reset from completed since it never actually completed fully)
4. Set outcome to null, completed_at to null (clear the false completion markers)
5. Write back the file
6. Validate: jq -e '.' data/tasks/active.jsonl

**Part B: Execute task-2026-03-23-001 via /task run**

Now run the reset task through the full pipeline. Use the /task command in Mode 3:

Run: /task run task-2026-03-23-001

This will:
- Find the task in active.jsonl (now status=pending)
- Dispatch to task-executor subagent (or inline fallback)
- Subagent executes all 6 STEPs including STEP 4 (Save Output) and STEP 5 (Complete Task)
- Output files created in data/tasks/task-2026-03-23-001/
- Task record updated with output_dir, status=completed, outcome
- Gmail draft created (since source_email is set) with draft_id recorded
- Feed entry logged

After /task run completes, verify:

```bash
ls data/tasks/task-2026-03-23-001/
jq 'select(.id=="task-2026-03-23-001") | {status, output_dir, draft_id, outcome}' data/tasks/active.jsonl
```

Expected:
- data/tasks/task-2026-03-23-001/ directory exists with summary.md (task_type=document-summary)
- Task record has output_dir="data/tasks/task-2026-03-23-001/", status="completed", outcome set
- If MCP is available: draft_id is populated (Gmail draft created for Greg Davenport)

**Part C: Execute one triage-suggested task (from Plan 03 auto-queue)**

Pick the first pending triage-suggestion task from active.jsonl. Run it:

1. Find a pending triage-suggestion task:
   ```bash
   jq -r 'select(.status=="pending" and .trigger=="triage-suggestion") | .id' data/tasks/active.jsonl | head -1
   ```

2. Run: /task run {that-task-id}

3. Verify output:
   ```bash
   ls data/tasks/{that-task-id}/
   jq 'select(.id=="{that-task-id}") | {status, output_dir, task_type, outcome}' data/tasks/active.jsonl
   ```

This proves both the manual task path (Part B) and the triage-suggestion task path (Part C) produce complete output through STEP 4 and STEP 5.

IMPORTANT: If the Task tool subagent dispatch fails (as it did in the original Plan 02 live verification), use the inline fallback: read .claude/agents/task-executor.md and execute its instructions directly. The fallback is documented in task.md and produces identical results. When using the inline fallback, you MUST execute all 6 STEPs including STEP 4 (Save Output) -- do not stop after STEP 3.
  </action>
  <verify>
    <automated>cd /Users/glenr/work/todo-list && test -d data/tasks/task-2026-03-23-001 && test -f data/tasks/task-2026-03-23-001/summary.md && OUTPUT_DIR=$(jq -r 'select(.id=="task-2026-03-23-001") | .output_dir' data/tasks/active.jsonl) && test "$OUTPUT_DIR" != "null" && echo "Task 001 output_dir: $OUTPUT_DIR" && COMPLETED_COUNT=$(jq -r 'select(.status=="completed" and .output_dir != null) | .id' data/tasks/active.jsonl | wc -l | tr -d ' ') && echo "Completed tasks with output_dir: $COMPLETED_COUNT" && test "$COMPLETED_COUNT" -ge 1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- data/tasks/task-2026-03-23-001/ directory exists
- data/tasks/task-2026-03-23-001/summary.md exists and has real content (not a stub)
- Task record for task-2026-03-23-001 has output_dir set to "data/tasks/task-2026-03-23-001/" (not null)
- Task record has status="completed" with outcome describing what was produced
- At least one completed task has output_dir populated (proving STEP 4 ran)
- Feed entry exists for task completion with output_dir in details
- If MCP available: draft_id is populated for the email-triggered task
  </acceptance_criteria>
  <done>At least one task has been executed through the full 6-step pipeline. Output directory and markdown files exist. Task record output_dir is populated. STEP 4 (Save Output) is proven functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete task execution pipeline end-to-end</name>
  <files>data/tasks/active.jsonl, data/feed.jsonl</files>
  <action>
Human verifies that both verification gaps from 03-VERIFICATION.md are now closed.

What was built:
- Gap 2 (Plan 03): Auto-queue pipeline populated active.jsonl with triage-suggestion tasks
- Gap 1 (Plan 04): Task execution produced output files in data/tasks/{task-id}/ with output_dir populated in task records

How to verify:

1. Check task output files exist:
   ```bash
   ls -la data/tasks/task-2026-03-23-001/
   cat data/tasks/task-2026-03-23-001/summary.md | head -20
   ```
   Should show a real document summary, not a placeholder.

2. Check task record integrity:
   ```bash
   jq '.' data/tasks/active.jsonl
   ```
   - task-2026-03-23-001 should have: status=completed, output_dir set, outcome set
   - Triage-suggestion tasks should exist with status=pending or completed

3. Check feed entries:
   ```bash
   jq 'select(.type=="task")' data/feed.jsonl
   ```
   Should show task completion entries with output_dir in details.

4. Run /status -- should show:
   - Pending task count with triage vs manual breakdown
   - Completed-today count reflecting the executed tasks
   - Top 3 pending tasks listed

5. Run /task -- should show pending triage-suggestion tasks (minus any that were executed)

6. Validate all data:
   ```bash
   scripts/validate-data.sh
   ```

Resume: Type "approved" if both gaps are closed, or describe remaining issues.
  </action>
  <verify>
    <automated>cd /Users/glenr/work/todo-list && test -d data/tasks/task-2026-03-23-001 && test -f data/tasks/task-2026-03-23-001/summary.md && jq -e 'select(.id=="task-2026-03-23-001" and .output_dir != null)' data/tasks/active.jsonl > /dev/null && echo "PASS: Gap 1 closed -- output files exist and output_dir populated"</automated>
  </verify>
  <done>Human confirmed both verification gaps are closed: auto-queue pipeline produces triage-suggestion tasks (Gap 2) and task execution produces output files with output_dir populated (Gap 1). Phase 3 verification score moves from 6/8 to 8/8.</done>
</task>

</tasks>

<verification>
1. ls data/tasks/task-2026-03-23-001/ -- directory exists with markdown file(s)
2. jq 'select(.id=="task-2026-03-23-001") | .output_dir' data/tasks/active.jsonl -- not null
3. jq 'select(.trigger=="triage-suggestion")' data/tasks/active.jsonl | jq -s 'length' -- 4+ tasks
4. jq 'select(.status=="completed" and .output_dir != null)' data/tasks/active.jsonl | jq -s 'length' -- at least 1
5. scripts/validate-data.sh -- all data validates
</verification>

<success_criteria>
- Gap 1 is closed: task execution produces output files in data/tasks/{task-id}/ directory, task record has output_dir populated (TASK-04, TASK-05 evidence)
- Gap 2 is closed (from Plan 03): active.jsonl contains triage-suggestion tasks (TASK-02 evidence)
- Both manual and triage-suggestion task execution paths produce complete output
- All verification failures from 03-VERIFICATION.md are resolved
</success_criteria>

<output>
After completion, create .planning/phases/03-task-execution/03-04-SUMMARY.md
</output>
