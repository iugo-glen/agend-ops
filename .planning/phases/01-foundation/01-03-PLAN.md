---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - CLAUDE.md
  - .claude/commands/status.md
  - .claude/commands/task.md
  - .claude/commands/feed.md
  - .claude/commands/triage-inbox.md
autonomous: true
requirements:
  - FOUND-04
must_haves:
  truths:
    - "CLAUDE.md contains MCP usage instructions directing Claude to use hardened-workspace only"
    - "CLAUDE.md contains data convention documentation (NDJSON paths, timestamp format)"
    - "CLAUDE.md lists all custom commands with descriptions"
    - "/status command file exists and references data/feed.jsonl and data/tasks/active.jsonl"
    - "/task command file exists and creates task records in data/tasks/active.jsonl"
    - "/feed command file exists and reads from data/feed.jsonl"
    - "/triage-inbox command file exists as a Phase 2 stub"
    - "Custom commands have correct frontmatter (allowed-tools, description)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project configuration with MCP rules, data conventions, command list"
      contains: "hardened-workspace"
    - path: ".claude/commands/status.md"
      provides: "Quick summary command"
      contains: "feed.jsonl"
    - path: ".claude/commands/task.md"
      provides: "Task creation command"
      contains: "active.jsonl"
    - path: ".claude/commands/feed.md"
      provides: "Activity feed viewer command"
      contains: "feed.jsonl"
    - path: ".claude/commands/triage-inbox.md"
      provides: "Email triage command stub for Phase 2"
      contains: "triage"
  key_links:
    - from: "CLAUDE.md"
      to: "hardened-workspace MCP"
      via: "MCP usage instructions"
      pattern: "mcp__hardened-workspace"
    - from: ".claude/commands/status.md"
      to: "data/feed.jsonl"
      via: "Reads feed for recent activity"
      pattern: "feed.jsonl"
    - from: ".claude/commands/task.md"
      to: "data/tasks/active.jsonl"
      via: "Appends task records"
      pattern: "active.jsonl"
    - from: ".claude/commands/feed.md"
      to: "data/feed.jsonl"
      via: "Reads and displays feed entries"
      pattern: "feed.jsonl"
---

<objective>
Enhance CLAUDE.md with project-specific instructions and create custom slash commands for /status, /task, /feed, and /triage-inbox.

Purpose: CLAUDE.md is the instruction set Claude reads at every session start. Custom commands give Glen the quick operations he needs (per D-07: /status, /task, /feed are required). This is the user interface to the entire system.

Output: Updated CLAUDE.md with MCP rules and data conventions; four custom command files in .claude/commands/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
Schemas from Plan 01 that commands must reference:

From schemas/feed-entry.json:
- Required fields: ts, type, summary, level, trigger
- type enum: triage, task, draft, system, command
- level enum: critical, info, debug
- trigger enum: manual, scheduled, hook

From schemas/task-record.json:
- Required fields: id, ts, status, description, trigger
- id pattern: task-YYYY-MM-DD-NNN
- status enum: pending, in-progress, completed, cancelled
- trigger enum: manual, triage-suggestion

Data paths from Plan 01:
- data/feed.jsonl (activity feed)
- data/tasks/active.jsonl (active tasks)
- data/triage/ (one JSONL per triage run)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enhance CLAUDE.md with MCP rules, data conventions, and command documentation</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current content -- must preserve existing GSD workflow, stack, and profile sections)
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 387-409 for CLAUDE.md enhancement content)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 for required commands, D-09 for Claude designing full command set)
    - schemas/feed-entry.json (field names for data conventions)
    - schemas/task-record.json (field names for data conventions)
  </read_first>
  <action>
Read the current CLAUDE.md first. It has existing GSD sections (project, stack, conventions, architecture, workflow, profile) that MUST be preserved.

Add the following NEW sections to CLAUDE.md. Insert them AFTER the `## Architecture` section and BEFORE the `## GSD Workflow Enforcement` section:

SECTION 1 -- ## MCP Server Usage:

CRITICAL: Use ONLY the `hardened-workspace` MCP server for Gmail and Drive access.
DO NOT use `claude.ai Gmail` or `claude.ai Google Calendar` connectors -- they can
send emails and route data through external servers.

When accessing Gmail or Drive, use tools prefixed with `mcp__hardened-workspace__`.

Security model: Claude can READ emails and CREATE drafts, but CANNOT send emails,
share files, create filters, or delete anything. This is enforced by the hardened MCP
fork and is the primary defense against prompt injection attacks.

SECTION 2 -- ## Data Conventions:

- Activity feed: Append to `data/feed.jsonl` (NDJSON, one JSON object per line)
  - Required fields: ts, type, summary, level, trigger
  - Types: triage, task, draft, system, command
  - All timestamps in ISO 8601 with timezone offset (e.g., 2026-03-23T10:30:00+10:00)
- Triage results: Create new file in `data/triage/` named by ISO timestamp (e.g., `2026-03-23T0900.jsonl`)
  - One record per email. Required fields: message_id, thread_id, from, subject, received, priority
- Task records: Append to `data/tasks/active.jsonl`
  - Required fields: id (task-YYYY-MM-DD-NNN), ts, status, description, trigger
- Schemas: See `schemas/` directory for full JSON Schema definitions of all record types
- Dashboard data: Run `scripts/build-dashboard-data.sh` to compile NDJSON into `docs/feed.json` and `docs/tasks.json`

SECTION 3 -- ## Commands:

Custom operations available as slash commands:

- `/status` -- Quick summary: recent activity count, triage runs, pending tasks
- `/task <description>` -- Create a new task or list pending tasks (no args)
- `/feed [count]` -- Show recent activity feed entries (default: 10)
- `/triage-inbox` -- Scan Gmail inbox and categorize emails (Phase 2 -- currently stub)

Do NOT modify any existing sections. The new sections are additive only.
  </action>
  <verify>
    <automated>grep -q "mcp__hardened-workspace__" CLAUDE.md && grep -q "Data Conventions" CLAUDE.md && grep -q "/status" CLAUDE.md && grep -q "GSD Workflow Enforcement" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `## MCP Server Usage` section with the text `mcp__hardened-workspace__`
    - CLAUDE.md contains `## Data Conventions` section with the text `data/feed.jsonl`
    - CLAUDE.md contains `## Commands` section listing /status, /task, /feed, /triage-inbox
    - CLAUDE.md still contains `## GSD Workflow Enforcement` (existing section preserved)
    - CLAUDE.md still contains `## Project` (existing section preserved)
    - CLAUDE.md still contains `## Technology Stack` (existing section preserved)
  </acceptance_criteria>
  <done>CLAUDE.md has MCP usage rules, data conventions with exact field names and paths, and a complete command reference</done>
</task>

<task type="auto">
  <name>Task 2: Create custom slash command files</name>
  <files>.claude/commands/status.md, .claude/commands/task.md, .claude/commands/feed.md, .claude/commands/triage-inbox.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 301-357 for command examples with frontmatter format)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 required commands, D-08 triage design discretion, D-09 full command set)
    - schemas/feed-entry.json (field definitions for feed entries)
    - schemas/task-record.json (field definitions and id pattern for task records)
  </read_first>
  <action>
Create the `.claude/commands/` directory and four command files. Each file uses YAML frontmatter (triple-dash delimited) followed by a markdown body.

FILE: .claude/commands/status.md (per D-07)
Frontmatter fields: description="Quick summary of current Agend Ops state", allowed-tools="Read, Bash(jq *), Bash(wc *), Bash(ls *), Bash(tail *)"
Body content:

Show a quick status summary for Agend Ops:

1. Recent Activity: Read last 5 entries from data/feed.jsonl using `tail -n 5 data/feed.jsonl | jq -s '.'`
2. Triage Runs: List files in data/triage/ sorted by date (most recent first) using `ls -t data/triage/*.jsonl 2>/dev/null | head -5`
3. Pending Tasks: Count entries with status "pending" in data/tasks/active.jsonl using `jq -r 'select(.status=="pending")' data/tasks/active.jsonl 2>/dev/null | wc -l`
4. System Health: Confirm hardened-workspace MCP server is available

Format output as a clean, scannable summary with counts and timestamps.
If any data file is empty or missing, report "No data yet" for that section.


FILE: .claude/commands/task.md (per D-07)
Frontmatter fields: description="Create or manage a task", allowed-tools="Read, Write, Edit, Bash(jq *), Bash(date *), Bash(wc *)"
Body content:

Create a new task record from the description: $ARGUMENTS

If arguments are provided:
1. Generate a task ID: task-{YYYY-MM-DD}-{NNN} where NNN is zero-padded sequence based on count of existing entries for today in data/tasks/active.jsonl
2. Get current timestamp in ISO 8601 with timezone offset (e.g., 2026-03-23T10:45:00+10:00)
3. Create a JSON object with these exact fields:
   - id: the generated task ID
   - ts: current ISO 8601 timestamp with timezone
   - status: "pending"
   - description: the $ARGUMENTS text
   - trigger: "manual"
   - source_email: null
   - outcome: null
   - completed_at: null
4. Append the JSON object as a single line to data/tasks/active.jsonl
5. Append a feed entry to data/feed.jsonl with: ts=now, type="task", summary="Created task: (description)", level="info", trigger="manual", details={"task_id":"(id)"}
6. Display the created task with its ID

If no arguments provided:
1. Read data/tasks/active.jsonl
2. Filter for entries where status is "pending" or "in-progress"
3. Display as a formatted list with ID, status, description, and creation time
4. If no tasks found, display "No active tasks."


FILE: .claude/commands/feed.md (per D-07)
Frontmatter fields: description="Show recent activity feed entries", allowed-tools="Read, Bash(jq *), Bash(tail *), Bash(wc *)"
Body content:

Display the most recent activity feed entries from data/feed.jsonl.

Default: last 10 entries. If $ARGUMENTS contains a number, show that many entries.

Steps:
1. Determine count: parse $ARGUMENTS for a number, default to 10
2. Read entries: `tail -n {count} data/feed.jsonl`
3. Parse each line as JSON
4. Format each entry showing:
   - Timestamp (formatted as relative time if today, absolute date otherwise)
   - Type icon: triage=inbox, task=checkbox, draft=pencil, system=gear, command=terminal
   - Summary text
   - Level (only show if "critical")
5. Group by date if entries span multiple days

If data/feed.jsonl is empty or does not exist, display "No activity recorded yet."


FILE: .claude/commands/triage-inbox.md (per D-08 -- Claude's discretion on design, stub for Phase 2)
Frontmatter fields: description="Scan Gmail inbox and categorize emails into priority buckets", allowed-tools="Read, Write, Edit, Bash(jq *), Bash(date *), mcp__hardened-workspace__*"
Body content:

PHASE 2 STUB -- This command will be fully implemented in Phase 2 (Email Triage).

When Phase 2 is complete, this command will:
1. Use hardened-workspace MCP to read unread and recent Gmail messages
2. Categorize each email into priority buckets: urgent, needs-response, informational, low-priority
3. Surface starred emails as highest-priority "needs my action" queue
4. Detect actionable items (contracts, invoices, meeting requests, deadlines)
5. Write triage results to data/triage/{timestamp}.jsonl
6. Log a feed entry to data/feed.jsonl
7. Display a summary of the triage results

For now, display this message:
"Triage inbox is not yet implemented. It will be available after Phase 2 (Email Triage) is complete.
Run /status to see current system state."

NOTE: Each command file must have YAML frontmatter delimiters (triple dashes on their own line) wrapping the description and allowed-tools fields, followed by the body content. See .planning/phases/01-foundation/01-RESEARCH.md lines 301-357 for the exact format.
  </action>
  <verify>
    <automated>test -f .claude/commands/status.md && test -f .claude/commands/task.md && test -f .claude/commands/feed.md && test -f .claude/commands/triage-inbox.md && grep -q "allowed-tools" .claude/commands/status.md && grep -q "active.jsonl" .claude/commands/task.md && grep -q "feed.jsonl" .claude/commands/feed.md && grep -q "hardened-workspace" .claude/commands/triage-inbox.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/status.md exists with frontmatter containing `allowed-tools:` and body referencing `data/feed.jsonl`
    - .claude/commands/task.md exists with frontmatter containing `allowed-tools:` and body referencing `data/tasks/active.jsonl`
    - .claude/commands/task.md body contains the task ID pattern `task-{YYYY-MM-DD}-{NNN}` or equivalent
    - .claude/commands/feed.md exists with frontmatter containing `allowed-tools:` and body referencing `data/feed.jsonl`
    - .claude/commands/triage-inbox.md exists with frontmatter containing `mcp__hardened-workspace__`
    - .claude/commands/triage-inbox.md body contains "PHASE 2 STUB" or equivalent stub indicator
    - All four command files have YAML frontmatter with description and allowed-tools fields
  </acceptance_criteria>
  <done>Four custom commands created: /status, /task, /feed are functional; /triage-inbox is a documented stub for Phase 2</done>
</task>

</tasks>

<verification>
1. `ls .claude/commands/` shows status.md, task.md, feed.md, triage-inbox.md
2. `grep -l "allowed-tools" .claude/commands/*.md | wc -l` returns 4
3. `grep "hardened-workspace" CLAUDE.md` returns matches in MCP section
4. `grep "Data Conventions" CLAUDE.md` returns a match
5. `grep "Commands" CLAUDE.md` returns a match listing all four commands
6. All existing CLAUDE.md sections (GSD Workflow, Technology Stack, Project) still present
</verification>

<success_criteria>
- CLAUDE.md has MCP usage rules preventing use of Anthropic official connectors
- CLAUDE.md has data conventions documenting all NDJSON paths and field names
- CLAUDE.md has command reference listing all four commands
- All existing CLAUDE.md content is preserved
- Four command files exist with proper frontmatter and actionable instructions
- /status, /task, /feed reference correct data file paths
- /triage-inbox is a clear stub indicating Phase 2 dependency
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
