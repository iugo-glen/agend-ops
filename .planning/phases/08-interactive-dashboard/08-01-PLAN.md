---
phase: 08-interactive-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/next.config.ts
  - dashboard/tsconfig.json
  - dashboard/Dockerfile
  - dashboard/.env.local.example
  - dashboard/src/app/layout.tsx
  - dashboard/src/app/globals.css
  - dashboard/src/app/login/page.tsx
  - dashboard/src/app/api/auth/[...nextauth]/route.ts
  - dashboard/src/app/api/data/[type]/route.ts
  - dashboard/src/app/api/actions/route.ts
  - dashboard/src/auth.ts
  - dashboard/src/middleware.ts
  - dashboard/src/lib/data.ts
  - dashboard/src/lib/queue.ts
  - dashboard/src/lib/types.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "Next.js dev server starts and serves pages at localhost:3000"
    - "Unauthenticated requests redirect to /login"
    - "Only glen@iugo.com.au can sign in (other Google accounts rejected)"
    - "API routes return parsed NDJSON data as JSON arrays"
    - "POST /api/actions appends an action to the queue file"
    - "Dockerfile builds a standalone Next.js image"
  artifacts:
    - path: "dashboard/src/auth.ts"
      provides: "Auth.js v5 config with Google provider and single-user lock"
      contains: "glen@iugo.com.au"
    - path: "dashboard/src/lib/data.ts"
      provides: "NDJSON file readers for all data types"
      exports: ["readNDJSON", "getFeedEntries", "getLatestTriage", "getTodos", "getInvoices", "getTasks"]
    - path: "dashboard/src/lib/queue.ts"
      provides: "Action queue writer"
      exports: ["queueAction", "QueuedAction"]
    - path: "dashboard/src/lib/types.ts"
      provides: "TypeScript interfaces from JSON schemas"
      exports: ["FeedEntry", "TriageRecord", "TaskRecord", "TodoRecord", "InvoiceRecord"]
    - path: "dashboard/src/app/api/data/[type]/route.ts"
      provides: "Data API routes for all 5 data types"
      exports: ["GET"]
    - path: "dashboard/src/app/api/actions/route.ts"
      provides: "Action queue POST endpoint"
      exports: ["POST"]
    - path: "dashboard/Dockerfile"
      provides: "Multi-stage Docker build for Coolify standalone deployment"
      contains: "standalone"
  key_links:
    - from: "dashboard/src/middleware.ts"
      to: "dashboard/src/auth.ts"
      via: "re-export auth as middleware"
      pattern: "export.*auth.*middleware"
    - from: "dashboard/src/app/api/data/[type]/route.ts"
      to: "dashboard/src/lib/data.ts"
      via: "import data readers"
      pattern: "import.*from.*@/lib/data"
    - from: "dashboard/src/app/api/actions/route.ts"
      to: "dashboard/src/lib/queue.ts"
      via: "import queue writer"
      pattern: "import.*from.*@/lib/queue"
user_setup:
  - service: google-oauth
    why: "Dashboard login requires Google OAuth"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console > APIs & Services > Credentials (reuse existing or create new OAuth client)"
      - name: GOOGLE_CLIENT_SECRET
        source: "Same OAuth client"
      - name: AUTH_SECRET
        source: "Generate with: openssl rand -base64 32"
    dashboard_config:
      - task: "Add redirect URI for local dev"
        location: "Google Cloud Console > Credentials > OAuth Client > Authorized redirect URIs: http://localhost:3000/api/auth/callback/google"
---

<objective>
Scaffold the Next.js application with authentication, data layer, API routes, action queue endpoint, and Dockerfile.

Purpose: Establish the full backend infrastructure so that UI components (Plan 02) can consume data via SWR hooks and submit actions via the queue API. This is the foundation that everything else builds on.

Output: A working Next.js app at dashboard/ with auth-protected API routes, NDJSON readers, action queue, and a Docker build ready for Coolify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interactive-dashboard/08-CONTEXT.md
@.planning/phases/08-interactive-dashboard/08-RESEARCH.md
@.planning/phases/04-dashboard/04-UI-SPEC.md
@schemas/feed-entry.json
@schemas/triage-record.json
@schemas/task-record.json
@schemas/todo-record.json
@schemas/invoice-record.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with auth, types, and data layer</name>
  <files>
    dashboard/package.json
    dashboard/next.config.ts
    dashboard/tsconfig.json
    dashboard/.env.local.example
    dashboard/src/auth.ts
    dashboard/src/middleware.ts
    dashboard/src/lib/types.ts
    dashboard/src/lib/data.ts
    dashboard/src/lib/queue.ts
    dashboard/src/app/layout.tsx
    dashboard/src/app/globals.css
    dashboard/src/app/login/page.tsx
    dashboard/src/app/api/auth/[...nextauth]/route.ts
    dashboard/src/app/page.tsx
  </files>
  <read_first>
    @schemas/feed-entry.json
    @schemas/triage-record.json
    @schemas/task-record.json
    @schemas/todo-record.json
    @schemas/invoice-record.json
    @.planning/phases/08-interactive-dashboard/08-RESEARCH.md (Architecture Patterns section)
    @.planning/phases/04-dashboard/04-UI-SPEC.md (Color and Typography sections)
  </read_first>
  <action>
1. Initialize Next.js project in /dashboard directory:
   ```bash
   cd dashboard
   npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-turbopack --import-alias "@/*"
   npm install next-auth@beta swr clsx date-fns
   ```

2. Create `dashboard/src/lib/types.ts` -- TypeScript interfaces derived directly from the JSON schemas:
   - `FeedEntry`: ts, type (union of enums), summary, level, trigger, details?, duration_ms?
   - `TriageRecord`: message_id, thread_id, from, from_name?, subject, received, category?, priority, starred?, has_attachments?, snippet?, action_items?, suggested_action?, client_name?, client_domain?, action_type?, draft_id?
   - `TaskRecord`: id, ts, status, description, trigger, source_email?, outcome?, completed_at?, task_type?, output_dir?, source_triage?, client_name?, draft_id?
   - `TodoRecord`: id, ts, status, text, priority, trigger, due_date?, category?, recurring?, completed_at?, linked_task_id?
   - `InvoiceRecord`: id, ts, status, client_name, amount?, currency?, tax_amount?, due_date?, description?, invoice_number?, po_number?, project_code?, payment_terms?, source_email_id?, xero_invoice_id?, trigger, amount_paid?, completed_at?
   - `QueuedAction`: id, ts, action (union: 'mark-paid' | 'complete-todo' | 'complete-task' | 'trigger-triage'), target_id, status ('queued' | 'completed' | 'failed'), requested_by, processed_at?, result?

3. Create `dashboard/src/lib/data.ts` per D-05 (direct file read from disk):
   - `DATA_ROOT` from `process.env.DATA_DIR || path.join(process.cwd(), '..', 'data')` (relative fallback for local dev)
   - `readNDJSON<T>(filePath)`: read file, split on newlines, JSON.parse each line. Return [] if file missing. Use `readFileSync` (files are tiny per research).
   - `getFeedEntries(limit=50)`: read 'feed.jsonl', slice last N, reverse (newest first)
   - `getLatestTriage()`: list files in 'triage/' dir, sort by filename (ISO timestamp), read the most recent one
   - `getTodos()`: read 'todos/active.jsonl'
   - `getInvoices()`: read 'invoices/active.jsonl'
   - `getTasks()`: read 'tasks/active.jsonl'

4. Create `dashboard/src/lib/queue.ts` per D-02 (queue for Claude):
   - `QUEUE_FILE` path from `DATA_ROOT/queue/actions.jsonl`
   - `queueAction(action, target_id, requested_by)`: creates QueuedAction with id=`action-{ISO timestamp}`, status='queued', appends as NDJSON line to queue file. Creates directory with `mkdirSync({recursive: true})`.
   - `getQueuedActions()`: read queue file, return array of QueuedAction

5. Create `dashboard/src/auth.ts` per D-08 (single user lock to glen@iugo.com.au):
   - Use Auth.js v5 pattern from research: NextAuth with Google provider
   - `signIn` callback: return `profile?.email === 'glen@iugo.com.au'`
   - Custom sign-in page: `/login`
   - Export: handlers, auth, signIn, signOut

6. Create `dashboard/src/middleware.ts`:
   - Re-export auth as middleware
   - Matcher: protect everything except /login, /api/auth, _next/static, _next/image, favicon.ico

7. Create `dashboard/src/app/api/auth/[...nextauth]/route.ts`:
   - Import handlers from @/auth, export as GET and POST

8. Create `dashboard/src/app/layout.tsx`:
   - Root layout with SessionProvider from next-auth/react wrapping children
   - Import globals.css
   - Metadata: title "Agend Ops", description "Operations Dashboard"
   - `<html lang="en">` with system font stack

9. Create `dashboard/src/app/globals.css` with CSS custom properties from UI-SPEC.md:
   - Light mode: --surface: #FFFFFF, --surface-secondary: #F7F6F3, --accent: #2383E2, --text-primary: #37352F, --text-secondary: #787774, --border: #E3E2DE, --destructive: #EB5757
   - Dark mode (@media prefers-color-scheme: dark): --surface: #191919, --surface-secondary: #252525, --accent: #529CCA, --text-primary: #FFFFFFCF, --text-secondary: #FFFFFF52, --border: #FFFFFF14
   - Semantic colors: --priority-urgent, --priority-needs-response, etc. from UI-SPEC
   - Include Tailwind directives (@tailwind base, components, utilities)
   - Import base font via body rule: font-family system-ui stack from UI-SPEC

10. Create `dashboard/src/app/login/page.tsx`:
    - Simple centered login page with "Agend Ops" title and "Sign in with Google" button
    - Use signIn("google") from next-auth/react
    - Style with Tailwind using the design tokens (warm Notion aesthetic per D-11)

11. Create `dashboard/src/app/page.tsx` as a minimal placeholder:
    - Server component that calls auth() to get session
    - Display "Welcome, {session.user.name}" and a sign out button
    - Placeholder text: "Dashboard loading..." (Plan 02 replaces this with KanbanBoard)

12. Update `dashboard/next.config.ts`:
    - Set `output: 'standalone'` for Docker deployment
    - Set `NEXT_TELEMETRY_DISABLED=1`

13. Create `dashboard/.env.local.example` documenting required env vars:
    - GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, AUTH_SECRET, NEXTAUTH_URL=http://localhost:3000, DATA_DIR (optional, defaults to ../data)
  </action>
  <verify>
    <automated>cd /Users/glenr/work/todo-list/dashboard && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js project builds without errors. Auth config, data layer, queue writer, and types all compile. Login page and placeholder main page exist.</done>
</task>

<task type="auto">
  <name>Task 2: API routes for data reads and action queue, plus Dockerfile</name>
  <files>
    dashboard/src/app/api/data/[type]/route.ts
    dashboard/src/app/api/actions/route.ts
    dashboard/Dockerfile
  </files>
  <read_first>
    @dashboard/src/lib/types.ts (created in Task 1)
    @dashboard/src/lib/data.ts (created in Task 1)
    @dashboard/src/lib/queue.ts (created in Task 1)
    @dashboard/src/auth.ts (created in Task 1)
    @.planning/phases/08-interactive-dashboard/08-RESEARCH.md (API Route and Dockerfile sections)
  </read_first>
  <action>
1. Create `dashboard/src/app/api/data/[type]/route.ts` per D-07:
   - Auth check: `const session = await auth(); if (!session) return 401`
   - DATA_MAP: feed -> 'feed.jsonl', tasks -> 'tasks/active.jsonl', triage -> special (latest file in dir), todos -> 'todos/active.jsonl', invoices -> 'invoices/active.jsonl'
   - For triage type: use `getLatestTriage()` from data.ts
   - For all others: use `readNDJSON()` with the mapped file path
   - Return JSON response with the data array
   - Handle unknown types with 400 error
   - Note: Next.js 16 uses `Promise<{ type: string }>` for dynamic route params -- handle accordingly

2. Create `dashboard/src/app/api/actions/route.ts` per D-01, D-02:
   - Auth check same as above
   - Parse request body: { action, target_id }
   - Validate action is one of: 'mark-paid', 'complete-todo', 'complete-task', 'trigger-triage'
   - For trigger-triage, target_id can be empty string
   - Call queueAction() from lib/queue.ts
   - Return { queued: true, id: entry.id }
   - Also add GET handler to return current queue status (pending actions count)

3. Create `dashboard/Dockerfile` (multi-stage build from research):
   - Stage 1 (dependencies): node:22-slim, WORKDIR /app, copy package*.json, npm ci
   - Stage 2 (builder): copy node_modules from stage 1, copy source, ENV NODE_ENV=production, npm run build
   - Stage 3 (runner): node:22-slim, copy standalone output + static + public from builder, USER node, EXPOSE 3000, CMD ["node", "server.js"]
   - Include NEXT_TELEMETRY_DISABLED=1 in builder and runner stages
   - Create .next directory with proper ownership before copying

4. Create `dashboard/.dockerignore`:
   - node_modules, .next, .env*.local, Dockerfile, .dockerignore, .git
  </action>
  <verify>
    <automated>cd /Users/glenr/work/todo-list/dashboard && npm run build 2>&1 | tail -5 && echo "--- Dockerfile check ---" && head -5 /Users/glenr/work/todo-list/dashboard/Dockerfile</automated>
  </verify>
  <done>API routes compile and are included in build output. GET /api/data/feed returns feed data, POST /api/actions accepts action requests. Dockerfile exists with multi-stage standalone build.</done>
</task>

</tasks>

<verification>
1. `cd dashboard && npm run build` completes without errors
2. `cd dashboard && npm run dev` starts server at localhost:3000
3. Visiting localhost:3000 redirects to /login (auth middleware working)
4. All TypeScript interfaces match their source JSON schemas
5. Dockerfile contains standalone output configuration
6. .env.local.example documents all required environment variables
</verification>

<success_criteria>
- Next.js app builds and runs in dev mode
- Auth middleware redirects unauthenticated users to /login
- API routes exist for all 5 data types (feed, triage, tasks, todos, invoices) per DASH-02
- Action queue POST endpoint exists per DASH-03
- Google OAuth config restricts to glen@iugo.com.au per DASH-01/D-08
- Dockerfile ready for Coolify deployment per DASH-01/D-06
- TypeScript types derived from all 5 NDJSON schemas
</success_criteria>

<output>
After completion, create `.planning/phases/08-interactive-dashboard/08-01-SUMMARY.md`
</output>
